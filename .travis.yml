language: node_js

services:
  - docker

node_js:
  - 8

cache:
  yarn: true
  directories:
    - node_modules

branches:
  only:
    - master

script:
  - npm run test

jobs:
  include:
    - stage: release
      node_js: lts/*
      script: npm run semantic-release
